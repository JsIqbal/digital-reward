const AuthLayout = ({ children }) => {
    return (
        <div className="bg-[#111827] p-0 m-0  w-full flex items-center justify-center h-full">
            {children}
        </div>
    );
};

export default AuthLayout;
